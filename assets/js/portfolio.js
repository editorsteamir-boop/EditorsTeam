(() => {"use strict";const $=id=>document.getElementById(id);const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));async function init(){const id=new URLSearchParams(location.search).get("id");try{const r=await fetch(`./data/editors.json?v=${Date.now()}`,{cache:"no-store"});if(!r.ok)throw 0;const data=await r.json();const editor=(Array.isArray(data)?data:[]).find(x=>String(x.id)===String(id));if(!editor)throw 0;$("portfolioName").textContent=editor.fullName||"ادیتور";$("portfolioBadge").textContent=editor.badge||"";$("portfolioAvatar").src=editor.image||"./assets/images/default-avatar.svg";document.title=`نمونه‌کارهای ${editor.fullName||"ادیتور"}`;const images=Array.isArray(editor.portfolioImages)?editor.portfolioImages:[];$("portfolioGallery").innerHTML=images.length?images.map((src,i)=>`<img class="gallery-img" src="${esc(src)}" alt="نمونه‌کار ${i+1}" data-portfolio-image>`).join(""):'<div class="portfolio-empty">هنوز تصویری برای نمونه‌کارهای این ادیتور ثبت نشده است.</div>';}catch{$("portfolioName").textContent="ادیتور پیدا نشد";$("portfolioGallery").innerHTML='<div class="portfolio-empty">اطلاعات این ادیتور در دسترس نیست.</div>';}}document.addEventListener("click",e=>{const img=e.target.closest("[data-portfolio-image]");if(img){$("portfolioModalImage").src=img.src;$("portfolioModal").hidden=false;$("portfolioModal").classList.add("active");}if(e.target.id==="portfolioModal"||e.target.id==="portfolioClose"){$("portfolioModal").classList.remove("active");$("portfolioModal").hidden=true;$("portfolioModalImage").src="";}});document.readyState==="loading"?document.addEventListener("DOMContentLoaded",init):init();})();